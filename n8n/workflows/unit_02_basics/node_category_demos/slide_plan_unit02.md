# Unit 02 投影片製作規劃書

> 根據 `README_教科書.md`（L01—L13）整理，共 25 張投影片。
> 每張標注：標題、核心概念、圖片建議、排版方式、對應課程。

---

## 排版通則

| 原則 | 做法 |
|------|------|
| **一張一概念** | 每張投影片只講一個核心觀念 |
| **圖 > 表 > 文** | 優先用圖解，其次表格，最後才用純文字 |
| **比喻先行** | 先放生活比喻圖，再放技術說明 |
| **6-6 原則** | 每張不超過 6 行，每行不超過 6 個詞 |
| **色彩系統** | 綠=成功/簡單、黃=注意/中等、紅=錯誤/困難、藍=n8n 主色 |
| **程式碼要大** | Code snippet 字體至少 20pt，有語法高亮 |
| **底部標注課程編號** | 每張右下角放 L01~L13 編號，方便對照 |

---

## 第一組：開場與定位（2 張）

---

### Slide 01 — n8n 是什麼？膠水層概念

- **對應課程：** 前置知識
- **核心概念：** n8n = 膠水層，負責「接收 → 處理 → 輸出」

#### 圖片建議

中央放射狀架構圖：

```
                    ┌─── Slack
                    │
CRM ──→            │
         n8n LOGO ──┼─── Google Sheets
LINE ──→            │
                    │
Webhook ──→         ┴─── Database
```

- 中間放 n8n logo（大）
- 左側 3 個輸入源，各用服務 logo（CRM、LINE、Webhook icon）
- 右側 4 個輸出目標，各用服務 logo（Slack、Sheets、Email、DB）
- 箭頭用管線/膠水瓶意象

#### 排版

- 上方 70%：放射狀架構圖
- 下方 30%：一行大字 ——「**接收 → 處理 → 輸出**」
- 背景色：淺藍灰

---

### Slide 02 — 膠水層的五種能力（課程 Roadmap）

- **對應課程：** 前置知識
- **核心概念：** 為什麼要學這些概念？每種能力對應哪些課程

#### 圖片建議

五行圖示表格：

| 圖示 | 能力 | 需要懂的概念 | 對應課程 |
|------|------|------------|---------|
| 👁 眼睛 icon | 看懂各系統的資料 | JSON、CSV、XML、RSS、HTML | L01、L02、L09 |
| 🤝 握手 icon | 跟各系統溝通 | API、HTTP、RESTful | L03、L08 |
| 🔑 鑰匙 icon | 證明自己有權限 | 認證、Token、Credential | L07 |
| 🔔 鈴鐺 icon | 被動等通知 | Webhook | L04 |
| 🕷 蜘蛛 icon | 對方沒開窗口 | 爬蟲（HTML Extract） | L09 |

#### 排版

- 全頁表格，左欄圖示要大（64px）
- 課程編號用 pill badge 呈現（藍底白字）
- 這張作為整門課的 roadmap，後續每張投影片都可回扣

---

## 第二組：JSON 與資料流（3 張）

---

### Slide 03 — JSON：系統溝通的共同語言

- **對應課程：** L01
- **核心概念：** JSON 是什麼、四條規則

#### 圖片建議

上半部 — 比喻圖：

```
┌──────────────┐          ┌──────────────────────────┐
│  點餐單       │          │ {                        │
│  品項：拿鐵    │    ＝    │   "item": "拿鐵",         │
│  數量：2      │          │   "qty": 2,              │
│  備註：少冰    │          │   "note": "少冰"          │
└──────────────┘          │ }                        │
 （中文格式）               └──────────────────────────┘
                           （JSON 格式）
```

下半部 — 四條規則卡片（2×2 grid）：

| 卡片 | 規則 | 範例 |
|------|------|------|
| 卡片 1（藍） | 大括號 `{}` = 一筆資料 | `{ ... }` |
| 卡片 2（綠） | `"key": value` = 欄位 | `"amount": 640000` |
| 卡片 3（黃） | 文字加引號，數字不用 | `"stage": "提案中"` |
| 卡片 4（紫） | 中括號 `[]` = 多筆 | `[{...}, {...}]` |

#### 排版

- 上半部 55%：比喻對照圖
- 下半部 45%：四格卡片 grid
- 卡片圓角、帶陰影

---

### Slide 04 — Expression 語法速查

- **對應課程：** L02
- **核心概念：** `{{ }}` 四種用法

#### 圖片建議

2×2 語法卡片 grid：

| 卡片顏色 | 用途 | 語法 | 範例 |
|---------|------|------|------|
| 淺藍 | 取值 | `{{ $json.欄位名 }}` | `{{ $json.salesRep }}` → 林志明 |
| 淺綠 | 數學運算 | `{{ $json.a * $json.b }}` | `{{ $json.amount * $json.probability / 100 }}` |
| 淺黃 | 判斷 | `{{ $json.x >= n }}` | `{{ $json.amount >= 1000000 }}` → true |
| 淺紫 | 文字組合 | `{{ $json.a }} — {{ $json.b }}` | 林志明 — 台積電 |

#### 排版

- 全頁 2×2 grid
- 每格：上方用途標題（粗體）、中間語法（code font）、下方範例
- 右下角標注「所有 Expression 都寫在 `{{ }}` 裡面」

---

### Slide 05 — 結構化 vs 非結構化資料

- **對應課程：** L02
- **核心概念：** 程式最愛結構化資料

#### 圖片建議

左右對比圖：

```
┌───── 結構化（報告 A）─────┐    ┌───── 非結構化（報告 B）────────┐
│ 業務：黃淑芬              │    │ 黃淑芬上週跟統一的案子進到      │
│ 客戶：統一企業             │    │ 合約審核了，金額大概八十幾萬，    │
│ 金額：816,000            │    │ 法務那邊已經在看了...            │
│ 階段：合約審核             │    │                              │
│                          │    │                              │
│ ✅ 程式一秒讀懂           │    │ ❌ 程式需要「理解中文」         │
└──────────────────────────┘    └──────────────────────────────┘
```

中間大大的 **VS**

底部比較表（橫向）：

| | 結構化資料 | 非結構化資料 |
|---|---|---|
| 比喻 | Excel 表格 | Word 文件 |
| 格式 | JSON、CSV、XML、RSS | HTML、PDF、圖片 |

#### 排版

- 上方 65%：左右對比圖，中間 VS
- 下方 35%：簡易比較表
- 底部一句結論：「**n8n 最喜歡結構化資料。拿到非結構化的，第一步就是轉換。**」

---

## 第三組：API 與 HTTP（3 張）

---

### Slide 06 — API：系統的服務窗口

- **對應課程：** L03
- **核心概念：** API = 櫃台窗口

#### 圖片建議

銀行櫃台示意圖（三層）：

```
┌────────┐     ┌──────────┐     ┌──────────────┐
│ 你      │ ──→ │ 櫃台窗口  │ ──→ │ 銀行內部系統   │
│（客戶）  │     │（API）    │     │（看不到）      │
└────────┘     └──────────┘     └──────────────┘

你只需要知道：
  ① 哪個窗口辦什麼事（API 的功能）
  ② 要帶什麼資料（API 的參數）
  ③ 會拿到什麼回覆（API 的回傳）
```

底部對照：

| 在 n8n 裡 | 說明 |
|-----------|------|
| 專用節點（Slack、Sheets、Notion） | 已經幫你包好的 API 窗口 |
| HTTP Request 節點 | 萬用窗口，什麼 API 都能打 |

#### 排版

- 上方 60%：三層箭頭圖（可用插畫風格）
- 中間 15%：三個要點（帶編號圓圈）
- 下方 25%：兩欄對照

---

### Slide 07 — HTTP 四大方法 + 狀態碼

- **對應課程：** L03
- **核心概念：** GET/POST/PUT/DELETE + 200/201/401/403/404

#### 圖片建議

上半部 — 餐廳四格漫畫：

| 格 | 場景 | HTTP 方法 |
|----|------|----------|
| 1 | 「我要看菜單」（看菜單圖） | **GET** — 拿資料 |
| 2 | 「我要點拿鐵」（點餐圖） | **POST** — 新增 |
| 3 | 「改成去冰」（修改圖） | **PUT/PATCH** — 修改 |
| 4 | 「取消訂單」（叉叉圖） | **DELETE** — 刪除 |

下半部 — 狀態碼色條：

```
🟢 200 成功「好的，辦好了」
🟢 201 建立成功「新帳戶開好了」
🔴 401 未授權「你是誰？」
🔴 403 禁止「認識你，但沒權限」
⚫ 404 找不到「沒有這個東西」
```

#### 排版

- 上半部 55%：1×4 橫向格子（圖+文字）
- 下半部 45%：五行色條（紅綠灰配色）
- 狀態碼數字要特別大（40pt+）

---

### Slide 08 — RESTful：URL 設計規範

- **對應課程：** L03
- **核心概念：** 看 URL + Method 就知道在做什麼

#### 圖片建議

置中表格：

| 你要做什麼 | HTTP 方法 | URL |
|-----------|----------|-----|
| 看所有商機 | `GET` | `/api/deals` |
| 看某一筆 | `GET` | `/api/deals/123` |
| 建立新商機 | `POST` | `/api/deals` |
| 修改商機 | `PUT` | `/api/deals/123` |
| 刪除商機 | `DELETE` | `/api/deals/123` |

上方口訣：「**URL = 資源名稱 / Method = 動作 → 組合起來就是一句話**」

#### 排版

- 上方 20%：口訣（大字）
- 中間 60%：表格（code font，左對齊）
- 下方 20%：一句結論「這就是 RESTful — 看網址就知道在幹嘛」
- 這張內容精簡，適合快速帶過（30 秒）

---

## 第四組：Trigger 與 Webhook（2 張）

---

### Slide 09 — 三種 Trigger 比較

- **對應課程：** L04
- **核心概念：** Manual / Schedule / Webhook

#### 圖片建議

三欄大圖示：

```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   🖱 手按      │  │   ⏰ 鬧鐘     │  │   📞 來電     │
│              │  │              │  │              │
│   Manual     │  │   Schedule   │  │   Webhook    │
│  你手動按按鈕  │  │  定時自動執行  │  │  外部系統通知  │
│              │  │              │  │              │
│  開發測試用    │  │  排程報表     │  │  LINE Bot    │
│              │  │  定時同步     │  │  CRM 通知    │
└──────────────┘  └──────────────┘  └──────────────┘
```

#### 排版

- 全頁三欄，每欄一個大 icon（128px）
- 下方兩行：場景 + 比喻
- 卡片帶圓角邊框和淺色背景

---

### Slide 10 — Webhook 深度解說

- **對應課程：** L04
- **核心概念：** 不是你去問，是對方主動通知你

#### 圖片建議

左右分割：

**左半部 — 比喻對比：**

```
❌ 沒有 Webhook（輪詢）：
你每 10 分鐘打電話問快遞：
「包裹到了嗎？」「還沒」
...重複 100 次...

✅ 有 Webhook：
你給快遞電話號碼
包裹到了 → 快遞主動打給你
```

**右半部 — 實際流程四步驟（垂直時間線）：**

```
Step 1  n8n 產生 Webhook URL
   ↓
Step 2  貼到 CRM 設定
   ↓
Step 3  業務建立新商機 → CRM 自動 POST
   ↓
Step 4  n8n 收到，自動執行
```

底部對照表：

| | API（你去問） | Webhook（他來通知） |
|---|---|---|
| 方向 | 你 → 對方 | 對方 → 你 |
| n8n 節點 | HTTP Request | Webhook Trigger |

#### 排版

- 左半部 50%：比喻故事（漫畫對話框風格）
- 右半部 50%：四步驟垂直時間線
- 底部 20%：比較表

---

## 第五組：流程控制（2 張）

---

### Slide 11 — IF vs Switch

- **對應課程：** L05
- **核心概念：** 二選一 vs 多選一

#### 圖片建議

左右對比大圖：

```
┌────── IF ──────┐        ┌────── Switch ──────────┐
│                │        │                        │
│    🚦 紅綠燈    │        │  🛣 高速公路匝道          │
│                │        │                        │
│   ┌── true     │        │   ┌── 提案中 → 安排拜訪   │
│   │            │        │   ├── 報價中 → 準備報價單  │
│ ──┤            │        │ ──┤                     │
│   │            │        │   └── 成交 → 啟動交付     │
│   └── false    │        │                        │
│                │        │                        │
│  金額 ≥ 100萬？ │        │  依「階段」路由           │
└────────────────┘        └────────────────────────┘
```

#### 排版

- 上方 70%：左右對比圖（含比喻 icon + 分支示意）
- 下方 30%：業務場景範例文字
- IF 用紅綠配色，Switch 用多彩色條

---

### Slide 12 — 資料轉換：Set vs Code

- **對應課程：** L06
- **核心概念：** Set 加欄位（不寫程式）→ Code 彙總（寫程式）

#### 圖片建議

水平流程圖（帶數量氣泡）：

```
  ●●●●●          ●●●●●              ●
  5 筆            5 筆               1 筆

Fake Sales  →  Set（加欄位）    →  Code Python（彙總）
               不寫程式              寫程式
               每筆加上              把 5 筆合成
               加權金額 + 佣金        一份報表
```

底部對照：

| | Set 節點 | Code 節點 |
|---|---|---|
| 需要寫程式嗎 | 不需要（拖拉設定） | 需要（Python/JS） |
| 輸入 vs 輸出 | N 筆 → N 筆 | N 筆 → 任意筆 |
| 適合 | 欄位計算、格式化 | 複雜彙總、分組統計 |

#### 排版

- 上方 60%：水平流程圖，數量用圓圈大小表示（5個小圈 → 1個大圈）
- 下方 40%：對照表
- 重點標示數量變化「5 → 5 → 1」

---

## 第六組：認證體系（3 張）

---

### Slide 13 — 為什麼需要認證

- **對應課程：** L07
- **核心概念：** 三個名詞：認證、憑證、Token

#### 圖片建議

上方情境圖：

```
你（n8n）走到 API 窗口
API 問：「請出示您的身分證」
沒有身分證 → 401 Unauthorized ❌
```

下方三欄卡片：

| 卡片 | 圖示 | 名詞 | 比喻 |
|------|------|------|------|
| 1 | 🔐 鎖 | 認證 Authentication | 出示證件的**過程** |
| 2 | 🪪 證件 | 憑證 Credential | 用來證明身分的**東西** |
| 3 | 🎫 票券 | Token | 一種**通行證**型的憑證 |

#### 排版

- 上方 40%：情境插畫（銀行窗口風格）
- 下方 60%：三欄卡片，每欄帶大 icon + 說明
- 卡片用不同淺色底（藍、綠、橘）

---

### Slide 14 — Token 與三種認證方式

- **對應課程：** L07
- **核心概念：** API Key vs Bearer Token vs OAuth 2.0

#### 圖片建議

上半部 — 遊樂園手環比喻：

```
Token = 遊樂園手環 🎡

  ✅ 刷手環就能玩（不用每次驗身分）
  ⏰ 有效期限（明天就不能用）
  🚫 有權限範圍（普通手環 ≠ VIP）
  ❌ 可以被作廢（違規停用）
```

下半部 — 三種認證比較表：

| | API Key | Bearer Token | OAuth 2.0 |
|---|---|---|---|
| 比喻 | 🏢 大樓門禁密碼 | 🏨 飯店房卡 | 👩‍💼 請秘書辦事 |
| 安全性 | 低 | 中 | 高 |
| 會過期嗎 | 通常不會 | 會 | 會（自動 Refresh） |
| 常見服務 | OpenAI | GitHub | Google、Slack、Notion |

#### 排版

- 上半部 45%：手環圖 + 四個特性
- 下半部 55%：三欄比較表
- 安全性用色條標示（紅→黃→綠）

---

### Slide 15 — n8n Credential：鑰匙包

- **對應課程：** L07 + L08
- **核心概念：** 為什麼要用 Credential，不要把 Token 寫在節點裡

#### 圖片建議

左右對比：

```
❌ 直接寫在節點                    ✅ 用 Credential
┌────────────────────┐          ┌────────────────────┐
│ Token: sk-abc123   │          │ Token: ********    │
│                    │          │                    │
│ ⚠️ 匯出會外洩      │          │ ✅ 加密儲存         │
│ ⚠️ 分享會外洩      │          │ ✅ 匯出不帶 Token   │
│ ⚠️ 換 Token 要改   │          │ ✅ 改一處全部更新    │
│    每個節點        │          │                    │
└────────────────────┘          └────────────────────┘
   紅色背景/紅色邊框                 綠色背景/綠色邊框
```

底部 — 常見服務認證類型表：

| 服務 | 認證類型 | 難度 |
|------|---------|------|
| OpenAI | API Key | ★ |
| Notion | Integration Token | ★ |
| LINE Bot | Channel Access Token | ★ |
| Slack | OAuth 2.0 | ★★ |
| Google Sheets | OAuth 2.0 | ★★ |

#### 排版

- 上方 60%：左右對比卡片（紅 vs 綠）
- 下方 40%：服務列表（帶 logo）

---

## 第七組：整合與 Fan-out（1 張）

---

### Slide 16 — 專用節點 vs HTTP + Fan-out 模式

- **對應課程：** L08
- **核心概念：** 一進多出、專用節點 vs 萬用 HTTP

#### 圖片建議

放射狀 Fan-out 圖：

```
                           ┌→ Slack 通知 🔔
                           │  （專用節點）
一筆成交資料 ──→ n8n ──→ ──┼→ CRM 更新 📊
                           │  （HTTP POST）
                           └→ Email 通知 ✉️
                              （HTTP POST）
```

底部對照表：

| | 專用節點 | 萬用 HTTP Request |
|---|---|---|
| 設定 | 選 Credential → 選頻道 → 打訊息 | 自己填 URL → 組 Body → 看文件 |
| 優點 | 簡單、有下拉選單 | 什麼 API 都能打 |
| 缺點 | 需先設定 OAuth | 要自己看 API 文件 |

#### 排版

- 上方 60%：Fan-out 放射圖（中心大、三端帶 icon）
- 下方 40%：兩欄比較表
- 標注「n8n 有 400+ 專用節點，找不到就用 HTTP Request」

---

## 第八組：資料格式（3 張）

---

### Slide 17 — 四種結構化格式一覽

- **對應課程：** L09
- **核心概念：** JSON / CSV / XML / RSS

#### 圖片建議

四欄卡片：

| 卡片 | 格式 | icon | 程式碼片段 | 一句話 |
|------|------|------|----------|--------|
| 1 | JSON | 👑 皇冠 | `{"key": "value"}` | 現代 API 標準，最主流 |
| 2 | CSV | 📊 表格 | `a,b,c` | 最簡單的表格，Excel 最愛 |
| 3 | XML | 🏛 政府 | `<tag>value</tag>` | 老前輩，政府/銀行還在用 |
| 4 | RSS | 🔔 鈴鐺 | `<rss><channel>...` | 網站最新消息自動推播 |

底部比較表：

| | JSON | CSV | XML | RSS |
|---|---|---|---|---|
| 檔案大小 | 小 | 最小 | 大 | 中等 |
| 能巢狀嗎 | ✅ | ❌ | ✅ | 固定結構 |
| n8n 支援 | 原生 | Convert to File | XML 節點 | RSS Trigger |

#### 排版

- 上方 55%：四欄卡片（各帶 icon + code snippet）
- 下方 45%：橫向比較表

---

### Slide 18 — HTML 與爬蟲

- **對應課程：** L09
- **核心概念：** HTML 是給人看的，不是給程式讀的

#### 圖片建議

上半部 — 左右對比：

```
✅ JSON（結構化）                    ❌ HTML（非結構化）
┌─────────────────────┐          ┌──────────────────────────┐
│ {                   │          │ <div class="deal-card">  │
│   "customer": "台積電",│         │   <h2>台積電</h2>         │
│   "amount": 1500000  │          │   <span>NT$ 1,500,000   │
│ }                   │          │   </span>               │
│                     │          │   <button>查看</button>   │
│ 程式直接讀 ✅         │          │ </div>                  │
└─────────────────────┘          │ 資料藏在排版裡 ❌          │
                                 └──────────────────────────┘
```

下半部 — 爬蟲流程圖（水平箭頭）：

```
HTTP GET 網頁 → HTML Extract（CSS Selector）→ Set 清理格式 → 乾淨 JSON ✅
```

附註：「JS 渲染的網頁？先按 F12 → Network → XHR → 找背後 API」

#### 排版

- 上方 55%：左右對比（綠 vs 紅邊框）
- 下方 30%：爬蟲流程圖（水平）
- 底部 15%：附註（灰色小字）

---

### Slide 19 — 決策樹：我該用什麼方式拿資料？

- **對應課程：** L09
- **核心概念：** 根據資料來源選擇最佳方法

#### 圖片建議

全頁決策流程圖（Flowchart）：

```
          我要的資料在哪裡？
               │
        ┌──────┼──────┬──────────┐
        ↓      ↓      ↓          ↓
    有 API？  RSS？  CSV/Excel？ 只有網頁
        │      │      │          │
     ┌──┴──┐   ↓      ↓       ┌──┴──┐
     ↓     ↓   RSS   Read     ↓     ↓
   JSON   XML  Trigger File  原始碼  JS 渲染
     ↓     ↓                 有資料？
   HTTP   HTTP              ┌──┴──┐
   Req    +XML              ↓     ↓
                          HTML    找背後
   🟢     🟢    🟢    🟢  Extract  API
  簡單   簡單  簡單  簡單   🟡      🟢最推薦
                          中等    ↓
                                找不到
                                 🔴
                              第三方爬蟲
```

路徑顏色：🟢 綠=簡單 / 🟡 黃=中等 / 🔴 紅=困難

#### 排版

- 全頁流程圖
- 用 Mermaid 或 draw.io 製作
- 每個終點標注難度色彩

---

## 第九組：AI 與錯誤處理（2 張）

---

### Slide 20 — AI Agent 架構

- **對應課程：** L10
- **核心概念：** Chat Trigger → Agent → LLM + Memory

#### 圖片建議

左半部 — 架構圖：

```
   💬 Chat Trigger
   （使用者輸入）
        │
        ↓
   🤖 AI Agent
   （推理引擎）
      ↗    ↖
  🧠        💾
 OpenAI   Memory
（大腦）  （記憶）
```

右半部 — System Prompt 三要素：

```
┌─────────────────────────────┐
│  System Prompt 設計重點        │
│                              │
│  📋 產品線與報價               │
│     → 完整價目表               │
│                              │
│  📏 回答規則                   │
│     → 推薦 1-3 項、附報價       │
│                              │
│  🌐 語言偏好                   │
│     → 用客戶語言回覆            │
└─────────────────────────────┘
```

#### 排版

- 左半部 50%：架構圖（大 icon）
- 右半部 50%：三要素卡片
- 底部：「System Prompt = 給 AI 的角色設定書」

---

### Slide 21 — Error Handling 與 SLA 監控

- **對應課程：** L11
- **核心概念：** Error Trigger + SLA 狀態分流

#### 圖片建議

上下兩條路徑：

```
── 路徑 1：Error Monitor ──────────────────────── 紅色主題
⚠️ Error Trigger → 📝 萃取錯誤資訊 → 📬 格式化異常通知

── 路徑 2：Deal SLA Monitor ───────────────────── 交通燈主題
📊 Fake Sales → 🧮 計算到期天數 → 🚦 IF 分流
                                    ├→ 🔴 OVERDUE（已逾期）→ 發送警報
                                    ├→ 🟡 URGENT（14天內）→ 發送警報
                                    └→ 🟢 ON_TRACK（正常）→ 記錄
```

#### 排版

- 上半部 45%：Error Monitor 路徑（紅色色調）
- 下半部 55%：SLA Monitor 路徑（紅黃綠三色）
- 分隔線用虛線

---

## 第十組：綜合演練（2 張）

---

### Slide 22 — 主管戰情室架構（L12）

- **對應課程：** L12
- **核心概念：** Fan-out → 平行計算 → Merge → 總報表

#### 圖片建議

全頁橫向流程圖：

```
Manual     Fake Sales   Enrich        Fan-out         Merge      Summary
Trigger →  (50 筆)  →  加權+風險  → ┬─ A 風險偵測 ─┐
                                    ├─ B 業務 KPI ─┤→ Merge →  📊 Executive
                                    └─ C 區域產品 ─┘          Summary

用到的概念標注：
  [L02] Expression    [L08] Fan-out    [L06] Code Python    [新] Merge
```

#### 排版

- 全頁橫向流程圖
- 每個節點下方標注對應課程（pill badge）
- 三條分支用不同顏色（紅=風險、藍=KPI、綠=區域）
- 最右邊放報表截圖示意

---

### Slide 23 — Code vs No-Code：L12 vs L13

- **對應課程：** L12 + L13
- **核心概念：** 同樣的結果，不同的方法

#### 圖片建議

左右對比：

```
┌────── L12 Code 版 ──────┐      ┌────── L13 No-Code 版 ────┐
│                          │      │                          │
│  🐍 Python Code 節點     │      │  📊 Summarize 節點       │
│                          │      │                          │
│  ✅ 靈活度高（任意邏輯）   │      │  ✅ 零程式碼（拖拉設定）   │
│  ✅ 複雜自訂報表          │      │  ✅ 像 Excel 樞紐分析     │
│  ⚠️ 需要會 Python        │      │  ⚠️ 只能用預設聚合函數    │
│                          │      │                          │
│  輸出：報表文字           │      │  輸出：Google Sheets     │
└──────────────────────────┘      └──────────────────────────┘
```

底部結論：「**選適合你的方法 — 會寫程式用 Code，不會就用 Summarize**」

#### 排版

- 全頁左右對比（左紫右綠）
- 底部一行結論（粗體）

---

## 第十一組：總結（2 張）

---

### Slide 24 — 全概念關係圖

- **對應課程：** 附錄 A
- **核心概念：** 所有概念的關聯一張圖

#### 圖片建議

同心圓 / 心智圖：

```
                 最外層：取得資料的三種方式
              ┌─── API（你去問）── HTTP Req + 專用節點
              │
   中層：     ├─── Webhook（他來通知）── Webhook Trigger
  通訊協定     │
              └─── 爬蟲（自己去看）── HTTP Req + HTML Extract
       ↑
     RESTful（設計規範）
       ↑
     認證 / Token（身分驗證）
       ↑
     HTTP 請求（溝通方式）
       ↑
     JSON（資料格式）
       ↑
     n8n（膠水層）── 核心

  底層基礎設施：n8n Credential（統一管理所有認證）
```

底部口訣：
> n8n 是膠水層，用 JSON 格式，透過 HTTP 請求，遵循 RESTful 的介面叫 API，
> 用 Token 通過認證，所有 Token 存在 Credential 裡。

#### 排版

- 全頁心智圖（從中心 n8n 向外展開）
- 每層用不同顏色
- 建議用 draw.io / Figma 製作

---

### Slide 25 — 口訣記憶 + 學習路線

- **對應課程：** 附錄 E + F
- **核心概念：** 記憶口訣 + 5 天學習路徑

#### 圖片建議

左半部 — 口訣（詩詞風格排版）：

```
n8n 是膠水，黏起所有系統
JSON 是語言，HTTP 是方法
REST 是規範，API 是窗口
Token 是通行證，Credential 是鑰匙包
Webhook 反著來，爬蟲自己找
```

右半部 — 學習路線進程條：

```
Day 1  ████░░░░░░  基礎操作
       L01 → L02
       Workflow、JSON、Expression

Day 2  ██████░░░░  串接外部
       L03 → L04 → L05
       API、HTTP、Webhook

Day 3  ████████░░  資料+認證
       L06 → L07 → L08
       Code、認證、Fan-out

Day 4  ██████████  進階應用
       L09 → L10 → L11
       格式、AI、Error

Day 5  ██████████  綜合演練
       L12 → L13
       Dashboard、No-Code
```

#### 排版

- 左半部 45%：口訣（大字，行距寬）
- 右半部 55%：進程條（帶課程編號）
- 背景用漸層色（從淺到深，代表進階）

---

## 附錄：投影片對照總表

| 投影片 | 標題 | 對應課程 | 核心圖片類型 |
|--------|------|---------|------------|
| 01 | n8n 膠水層概念 | 前置 | 放射狀架構圖 |
| 02 | 五種能力 Roadmap | 前置 | 圖示表格 |
| 03 | JSON 共同語言 | L01 | 比喻對照 + 四格卡片 |
| 04 | Expression 語法 | L02 | 2×2 語法卡片 |
| 05 | 結構化 vs 非結構化 | L02 | 左右對比 |
| 06 | API 服務窗口 | L03 | 三層箭頭圖 |
| 07 | HTTP 方法 + 狀態碼 | L03 | 四格漫畫 + 色條 |
| 08 | RESTful 規範 | L03 | 置中表格 |
| 09 | 三種 Trigger | L04 | 三欄大圖示 |
| 10 | Webhook 深度解說 | L04 | 比喻對比 + 流程 |
| 11 | IF vs Switch | L05 | 左右對比分支圖 |
| 12 | Set vs Code | L06 | 水平流程 + 數量氣泡 |
| 13 | 為什麼需要認證 | L07 | 情境圖 + 三欄卡片 |
| 14 | Token 與認證方式 | L07 | 比喻 + 比較表 |
| 15 | Credential 鑰匙包 | L07-L08 | 紅綠對比卡片 |
| 16 | Fan-out + 專用 vs HTTP | L08 | 放射圖 + 比較表 |
| 17 | 四種結構化格式 | L09 | 四欄卡片 + 比較表 |
| 18 | HTML 與爬蟲 | L09 | 對比 + 爬蟲流程 |
| 19 | 決策樹 | L09 | 全頁 Flowchart |
| 20 | AI Agent 架構 | L10 | 架構圖 + 三要素 |
| 21 | Error + SLA 監控 | L11 | 雙路徑流程圖 |
| 22 | 主管戰情室架構 | L12 | 全頁橫向流程圖 |
| 23 | Code vs No-Code | L12-L13 | 左右對比 |
| 24 | 全概念關係圖 | 附錄 A | 心智圖 |
| 25 | 口訣 + 學習路線 | 附錄 E+F | 口訣 + 進程條 |

---

## 附錄：工作坊練習題（2 題）

> 基於 L01–L09 所教內容設計，學生在 n8n 畫布上從零建置。
> 預估每題 15–20 分鐘，含助教巡場時間。

---

### 練習一：AI 科技新聞篩選器

**情境故事：**
你是新創公司的 PM，老闆每天早上要你整理「跟 AI 有關的科技新聞」。
與其每天手動瀏覽網站，不如讓 n8n 自動幫你篩選！

**完成目標：**
從科技新報 RSS 抓取新聞 → 只保留標題或分類含「AI」的文章 → 輸出乾淨的摘要清單

**涵蓋知識點：**

| 步驟 | 用到的概念 | 對應課程 |
|------|-----------|---------|
| 1. 手動觸發 | Manual Trigger | L01 |
| 2. HTTP GET 抓 RSS | HTTP Request 節點、URL、RSS 格式 | L03、L09 |
| 3. Code 節點解析 XML | Code 節點、JavaScript、資料轉換 | L06、L09 |
| 4. IF 節點篩選 | 條件判斷、Expression 語法 | L02、L05 |
| 5. Set 節點整理輸出 | 欄位重組、資料清洗 | L06 |

**學生操作步驟：**

```
Manual Trigger
      ↓
HTTP Request（GET https://technews.tw/feed/）
      ↓
Code（解析 XML，取出 title / link / categories）
      ↓
IF（判斷：title 或 categories 包含 "AI"）
  ├─ true  → Set（輸出：標題、連結、分類）
  └─ false → 不處理
```

**提示卡片（助教視學生狀況發放）：**

- 提示 1：HTTP Request 的 URL 填 `https://technews.tw/feed/`，不需要改其他設定
- 提示 2：RSS 回來的是 XML 字串，藏在 `$input.first().json.data` 裡面
- 提示 3：Code 節點可以用 regex `/<item>([\s\S]*?)<\/item>/g` 把每篇文章挖出來
- 提示 4：IF 節點的條件可以用 `{{ $json.title.includes("AI") || $json.categories.includes("AI") }}`
- 提示 5：Set 節點勾選「Keep Only Set」，只保留你要的 3 個欄位

**驗收標準：**
- ✅ 執行後 IF 的 true 分支有輸出（至少 3–5 篇 AI 相關新聞）
- ✅ 最終輸出只有 title、link、categories 三個欄位
- ✅ false 分支的文章標題都不含 AI

**延伸挑戰（完成太快的同學）：**
- 把 Manual Trigger 換成 Schedule Trigger，設定每天早上 9:00 自動執行（L04）
- 在 Set 節點加一個欄位 `fetched_at`，值是 `{{ $now.format("YYYY-MM-DD HH:mm") }}`

---

### 練習二：匯率警報看板

**情境故事：**
你計劃暑假去日本旅遊，想盯著日圓匯率。
當美元兌日圓超過 155（日圓貶值 = 換匯划算）時，產生一條「好消息」提醒；
否則產生「再等等」提醒。順便把台幣、韓元的匯率一起整理出來。

**完成目標：**
抓取即時匯率 → 篩選台幣 / 日圓 / 韓元 → 判斷日圓是否超過門檻 → 輸出結果

**涵蓋知識點：**

| 步驟 | 用到的概念 | 對應課程 |
|------|-----------|---------|
| 1. 手動觸發 | Manual Trigger | L01 |
| 2. HTTP GET 抓匯率 | HTTP Request 節點、JSON API | L03、L09 |
| 3. Code 節點篩選 | Code 節點、物件取值、陣列操作 | L02、L06 |
| 4. IF 節點判斷 | 數值比較、Expression | L02、L05 |
| 5. Set 節點格式化 | 欄位組合、文字串接 | L06 |

**學生操作步驟：**

```
Manual Trigger
      ↓
HTTP Request（GET https://tw.rter.info/capi.php）
      ↓
Code（從 200+ 幣別中取出 USDJPY、USDTWD、USDKRW）
      ↓
IF（判斷：USDJPY 匯率 > 155）
  ├─ true  → Set（輸出：「🎌 好消息！日圓便宜了，現在 1 USD = ¥{{匯率}}」）
  └─ false → Set（輸出：「⏳ 再等等，日圓還沒到甜蜜點，現在 1 USD = ¥{{匯率}}」）
```

**提示卡片（助教視學生狀況發放）：**

- 提示 1：HTTP Request 的 URL 填 `https://tw.rter.info/capi.php`，這個 API 不需要認證
- 提示 2：回傳的 JSON 是一個大物件，日圓匯率在 `$json.USDJPY.Exrate`
- 提示 3：Code 節點範例 —
  ```javascript
  const raw = $input.first().json;
  return [{
    json: {
      jpy_rate: raw.USDJPY.Exrate,
      twd_rate: raw.USDTWD.Exrate,
      krw_rate: raw.USDKRW.Exrate,
      updated: raw.USDJPY.UTC
    }
  }];
  ```
- 提示 4：IF 條件填 `{{ $json.jpy_rate > 155 }}`
- 提示 5：Set 節點用 Expression 組文字 — `日圓匯率 {{ $json.jpy_rate }}，台幣 {{ $json.twd_rate }}`

**驗收標準：**
- ✅ 執行後 Code 節點輸出包含 jpy_rate、twd_rate、krw_rate 三個數值
- ✅ IF 節點根據當前匯率正確走 true 或 false 分支
- ✅ 最終 Set 節點輸出包含人類可讀的提醒訊息

**延伸挑戰（完成太快的同學）：**
- 加一個 Switch 節點（L05），根據匯率區間輸出不同訊息：< 145「太貴」、145–155「普通」、> 155「便宜」
- 在 Code 節點多加幾個幣別（EUR、THB、GBP），做成完整的旅遊匯率看板
- 把結果用 Fan-out 同時送到兩個 Set 節點，模擬「同時通知 LINE + Email」（L08）

---

### 練習題設計原則

| 原則 | 說明 |
|------|------|
| **真實資料** | 使用台灣學生熟悉的網站和 API（科技新報、即時匯率） |
| **免認證** | 兩個 API 都不需要 API Key，降低環境設定門檻 |
| **漸進提示** | 5 張提示卡片由易到難，助教視學生卡關程度逐步釋放 |
| **明確驗收** | 每題 3 個 checkbox，學生和助教都能快速確認是否完成 |
| **有延伸** | 進度快的同學有額外挑戰，不會無聊等待 |
| **覆蓋完整** | 兩題合計覆蓋 L01–L09 的核心概念（見下表） |

**兩題覆蓋度對照：**

| 課程 | 概念 | 練習一 | 練習二 |
|------|------|:------:|:------:|
| L01 | Workflow 基礎、Manual Trigger | ✅ | ✅ |
| L02 | Expression 語法、JSON 取值 | ✅ | ✅ |
| L03 | HTTP Request、GET 方法 | ✅ | ✅ |
| L04 | Schedule Trigger | 延伸 | — |
| L05 | IF 條件判斷 | ✅ | ✅ |
| L06 | Set 節點、Code 節點 | ✅ | ✅ |
| L07 | 認證概念 | — | （API 免認證，但可口頭補充） |
| L08 | Fan-out 整合 | — | 延伸 |
| L09 | RSS/JSON/HTML 格式、ETL | ✅ | ✅ |
